# v2.1

- figure out units stuff
  - load /signalk/v1/unitpreferences/active -> store in appState
  - override types (distance -> depth)
    - environment.depth.belowSurface
    - environment.depth.belowKeel
    - environment.tide
- override types (distance -> length)
    - navigation.anchor.maxRadius

# LONG TERM

- different anchor shapes (arc, polygon, etc)
- custom boat icon upload
- possible glitch filter - filter any moves that are over X speed